/*
 *	@file		StringsAndArrays_Problem9.hpp
 *  @date		2020-12-19
 *	@author		Teran Bukenberger
 *
 *  @brief		You are given a function is_sub_string() which checks if one word
 *				is a substring of another. Given two strings, str1 and str2, write code
 *				to check if str2 is a rotation of str1 using only one call to is_sub_string().
 *				(i.e., "waterbottle" is a rotation of "erbottlewat").
 * 
 *				NOTE: is_sub_string() is already included in the header, so you can call it here.
 */

#include "StringsAndArrays_Problem9.hpp"

 /* Include any headers you wish to use */

bool string_rotation( const std::string& str1, const std::string& str2 ) {

	// TODO...

	return true;
}